[package]
name = "caterpillar-voting-core"
version = "0.1.0"
readme = "README.md"
description = "Collection of cryptographic primitives for verifiable elections."

[dependencies]
curve25519-dalek.workspace =  true
rand_core.workspace = true
zeroize.workspace = true

[dev-dependencies]
rand = { workspace = true }
